[pytest]
addopts = --import-mode=importlib
pythonpath = .
# Avoid collecting tests from bundled environments or dependencies.
norecursedirs = .venv node_modules
testpaths =
    # Keep default test discovery scoped to repo-owned suites.
    tests
    intelligence
    demo_secure_onboarding
    module_communication
    test_config.py
